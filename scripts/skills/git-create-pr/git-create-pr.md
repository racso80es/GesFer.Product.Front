# Cápsula: git-create-pr

`bin/git_create_pr.exe`. Envelope v2.

**Request:** `branch`, `base`, `title`, `body`, `bodyFile`, `pushFirst` (default true). Requiere `gh` en PATH y autenticación (`gh auth login`).
