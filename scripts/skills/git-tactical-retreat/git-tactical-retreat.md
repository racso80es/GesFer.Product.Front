# Cápsula: git-tactical-retreat

`bin/git_tactical_retreat.exe`. Envelope v2.

**Request:** al menos uno de `hardReset` o `cleanUntracked` (boolean). Si cualquiera es true, `confirmDestructive` debe ser true. `target` para reset (default `HEAD`).
