# Cápsula: git-sync-remote

`bin/git_sync_remote.exe`. Envelope v2.

**Request:** `remote` (string, default `origin`). `branch` (opcional): si se indica y no es la actual, hace checkout antes de `git pull <remote>`.
