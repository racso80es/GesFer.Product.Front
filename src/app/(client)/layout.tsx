/**
 * Layout del grupo de rutas de cliente
 * Layout limpio, sin sidebar, pensado para el usuario final
 */
export default function ClientGroupLayout({ children }: { children: React.ReactNode }) {
  return <>{children}</>;
}
