# Auditoría de Calidad y Mantenibilidad

## 1. Métricas de Salud (0-100%)
Arquitectura: 100% | Nomenclatura: 100% | Estabilidad Async: 100%

## 2. Pain Points (🔴 Críticos / 🟡 Medios)
Hallazgo: Terminología prohibida ("empresa") detectada en el frontend.

Ubicaciones:
- src/app/[locale]/page.tsx (Línea 90)
- src/app/[locale]/my-company/page.tsx (Línea 93)
- src/app/[locale]/login/page.tsx (Línea 14)
- src/__tests__/app/login/page.test.tsx (Línea 6)
- src/__tests__/integration/system-integrity.test.ts (Línea 21)
- src/lib/types/api.ts (Línea 158)

## 3. Acciones Kaizen (Hoja de Ruta para el Executor)
Se deben realizar las siguientes correcciones para eliminar la terminología prohibida y adaptar la app:
1. Reemplazar "empresas" por "organizaciones" en `src/app/[locale]/page.tsx`.
2. Reemplazar "empresa" por "organización" en `src/app/[locale]/my-company/page.tsx` y en comentarios de `src/lib/types/api.ts`.
3. Refactorizar `src/app/[locale]/login/page.tsx` para que `getDefaultLoginCredentials` no tenga credenciales de respaldo codificadas que se puedan exponer en producción (debe devolver cadenas vacías si no hay variables de entorno).
4. En los tests (`page.test.tsx` y `system-integrity.test.ts`), utilizar `DEMO_COMPANY_NAME` o valores demo actualizados ("Organización Demo") para que coincidan con la limpieza.

Definition of Done (DoD):
- El script de auditoría `scripts/audit_frontend_daily.py` no debe arrojar fallas críticas.
- La app debe seguir compilando (`npm run build`).
- Todos los tests deben pasar (`npm run test:all`).
