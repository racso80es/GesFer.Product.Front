# Especificación
- Archivos a modificar: componentes de `src/components/` y `src/app/` que utilizan `console.*`.
- Test a modificar: `src/__tests__/components/ui/overlay-fix.test.tsx` para mockear el logger.
