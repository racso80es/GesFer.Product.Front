---
id: "kaizen-e2e-auth-selectors-spec"
---
# Specification
1. Locate all instances of 'usuario' and 'contraseña' used within the e2e test auth flows.
2. Convert references to 'username', 'password', 'user', or 'admin' depending on context to align with the rest of the application.
3. Ensure `TEST-IDS.md` data-testid registry reflects `login-username-input` and `login-password-input`.
