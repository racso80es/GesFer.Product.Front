---
feature: kaizen-clean-code
phase: plan
---
# Plan
1. Ejecutar script Node.js para reemplazar `as any;` por `as unknown as ReturnType<typeof window.getComputedStyle>;` en `src/__tests__/components/ui/overlay-fix.test.tsx`.
