---
title: "Actualización de selectores Auth en pruebas E2E (S+)"
id: "kaizen-e2e-auth"
---
# Implementación
- En `src/tests/e2e/logging-persistence.spec.ts`: Se cambió `usuario-inexistente` por `non-existent-user`.
- En `src/tests/e2e/logs_purge_logic.spec.ts`: Se cambió `con usuario Admin` por `con Admin`.
- En `src/tests/e2e/admin_logs.spec.ts`: Se cambiaron comentarios que decían `El usuario` y `el usuario` a `El administrador` y `el administrador`.
