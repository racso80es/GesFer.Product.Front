---
id: actualizacion_e2e_auth
type: implementation
status: active
---
# Implementación: Actualización de selectores Auth

1. Reemplazar `usuario-inexistente` por `username-inexistente` en `src/tests/e2e/logging-persistence.spec.ts`.
2. Reemplazar texto del test y comentarios en `src/tests/e2e/logs_purge_logic.spec.ts` de `usuario` a `username`.
3. Reemplazar texto de comentarios en `src/tests/e2e/admin_logs.spec.ts` de `usuario` a `admin` (refiriéndose a la entidad).
