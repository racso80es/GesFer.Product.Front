---
title: Validación de Actualización de README
type: validation
feature_id: actualizacion-readme
---
# Validación

Se ha ejecutado lo siguiente:

- `npm install`: Instaladas dependencias.
- `npm run lint`: Se ha ejecutado sin errores o avisos.
- `npm run build`: La construcción optimizada ha finalizado sin problemas y no hubo errores de sintaxis en `src/`.

Los cambios han sido en su mayoría a nivel de archivos Markdown. Los únicos archivos modificados que tienen implicación directa al repositorio son `README.md` de la raíz y `docs/testing/testing-guide.md`. La eliminación de los archivos intermedios de `src` se ejecutó sin romper nada.